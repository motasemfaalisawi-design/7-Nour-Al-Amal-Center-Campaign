# CLAUDE.md

## سياق المشروع
المجلد `tender-platform/` يحوي منصة «توريد» — تجميع مناقصات وعروض أسعار فلسطينية مع تنبيهات مفلترة.
المرجع الشامل: `tender-platform/ops/RUNBOOK.md` (جرد الحسابات، أوامر التشغيل، خريطة الكود، القرارات الثابتة).
الدراسة والقرارات: `tender-platform/docs/phase0-study.md`.

## قواعد التخاطب مع المالك
- الرد بالعربية دائماً (لهجة فلسطينية ودّية مفهومة).
- المالك غير مبرمج: اشرح بلغة أعمال، والخطوات اليدوية مرقمة خطوة-خطوة.
- **تنسيق ضد كسر الاتجاه (مهم):** واجهة المالك تعرض النص باتجاه يسار-يمين، فأي كلمة لاتينية أو رمز داخل جملة عربية يبعثر السطر عنده. لذلك: اكتب جملاً عربية صافية، وضع كل قيمة تقنية أو رابط أو اسم لاتيني في سطر مستقل منفرد (يفضَّل داخل علامات كود). تجنّب الأقواس المختلطة والرموز اللاتينية وسط الجمل.

## قواعد تقنية ثابتة
- الريبو عام: يُمنع وضع أي توكن أو سر في الملفات — الأسرار في متغيرات بيئة محلية أو أسرار GitHub فقط.
- بيانات المشتركين (`tender-platform/data/subscribers.json`) خارج git للخصوصية.
- الملفات المولّدة (`site/dist/`، `data/tenders.json`) عند تعارض git: يُعاد توليدها لا تُدمج يدوياً.
- فرع العمل: `claude/gaza-market-business-2n74o6`.
